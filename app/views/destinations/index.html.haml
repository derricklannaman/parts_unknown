.jumbo-header
  Explore Your Destination


%section#fly-over-map
  #map3d

%ul#location-list
  - @destinations.each do |place|
    %li
      = link_to place.name, destination_path(place.id), remote: true, class: 'destination'



